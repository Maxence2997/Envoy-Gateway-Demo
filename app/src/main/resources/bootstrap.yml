spring:
  application:
    name: App-Service
  cloud:
    consul:
      host: ${SPRING_CLOUD_CONSUL_HOST:localhost}
      port: 8500
      discovery:
        register: true
        prefer-ip-address: true

server:
  port: 8081

grpc:
  server:
    port: 9090